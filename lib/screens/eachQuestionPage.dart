import 'package:flutter/material.dart';

class EachQuestionPage extends StatelessWidget {
  const EachQuestionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}
